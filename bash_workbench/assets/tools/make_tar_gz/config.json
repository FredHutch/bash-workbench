{
    "name": "Create Compressed Archive (tar.gz)",
    "description": "Combine all of the files in a folder into a single gzip-compressed tarball",
    "args": {
        "archive": {
            "help": "Base name of archive file (to which .tar.gz will be appended)",
            "wb_env": "ARCHIVE",
            "wb_type": "string",
            "required": true
        },
        "target": {
            "help": "Directory targeted for compression (all subfolders will be included recursively).",
            "wb_env": "TARGET",
            "wb_type": "folder",
            "nargs": 1
        }
    }
}